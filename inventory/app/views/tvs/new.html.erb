<h1>Add a TV Season!</h1>

<!-- generally no equal sign for a loop, but the exception is for forms -->
<!-- it is rails convention to loop through a form with an f -->
<%= form_for @tv do |f| %>
    Title: <%= f.text_field :title %><br>
    Season: <%= f.text_field :season %><br>
    Year: <%= f.number_field :year %><br>
    Show Runner: <%= f.text_field :show_runner %><br>
    Network: <%= f.text_field :network %><br>
    Format: <%= f.radio_button :format, 'DVD' %><%= f.label :format, 'DVD', value: 'dvd' %>
    <%= f.radio_button :format, 'Blu-Ray' %><%= f.label :format, 'Blu-Ray', value: 'bluray' %><br>
    Loaned Out?: <%= f.check_box :loaned_out %><br><br>

    <%= f.submit 'Add this TV season!' %>
<% end %> <!-- you don't need an equal sign in the end tag -->